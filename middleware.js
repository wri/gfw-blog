// import { NextResponse } from 'next/server'

// eslint-disable-next-line
export default function middleware(request) {
  // eslint-disable-next-line
  console.log('possible middleware to use with tags');
}
